name: Update Hagezi Blocklists
on:
  schedule:
    - cron: '0 */2 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Fetch and convert Pro Mini
        run: |
          curl -s https://hagezi-mirror.dnsbunker.org/wildcard/pro.mini-onlydomains.txt \
            | grep -v '^\s*#\|^\s*$' \
            | { echo "payload:"; sed "s/^/  - '+./; s/$/'/" ; } > pro-mini.yaml

      - name: Fetch and convert Pro Plus Mini
        run: |
          curl -s https://hagezi-mirror.dnsbunker.org/wildcard/pro.plus.mini-onlydomains.txt \
            | grep -v '^\s*#\|^\s*$' \
            | { echo "payload:"; sed "s/^/  - '+./; s/$/'/" ; } > pro-plus-mini.yaml

      - name: Fetch and convert Apple Native Tracker
        run: |
          curl -sL https://hagezi-mirror.dnsbunker.org/wildcard/native.apple-onlydomains.txt \
            | grep -v '^\s*#\|^\s*$' \
            | { echo "payload:"; sed "s/^/  - '+./; s/$/'/" ; } > apple-native-tracker.yaml

      - name: Fetch and convert Multi Light
        run: |
          curl -sL https://hagezi-mirror.dnsbunker.org/wildcard/light-onlydomains.txt \
            | grep -v '^\s*#\|^\s*$' \
            | { echo "payload:"; sed "s/^/  - '+./; s/$/'/" ; } > multi-light.yaml

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add pro-mini.yaml pro-plus-mini.yaml apple-native-tracker.yaml multi-light.yaml
          git diff --staged --quiet || git commit -m "Update blocklists $(date -u +%Y-%m-%d)"
          git push
